# Kasir TokoMu — Deploy Instructions
# Environment ini sudah dikonfigurasi sesuai workspace aktual proyek.
# Copy isi ini langsung ke file .env di folder warungos/

# ── Database ────────────────────────────────────────────────────────────────
# Cloud PostgreSQL (InsForge) — database aktual proyek ini
DATABASE_URL=postgresql://postgres:0a8d012736ad8dabcd543d58c94fed1c@rnuh6nq3.us-east.database.insforge.app:5432/insforge?sslmode=verify-full

# ── Better Auth ─────────────────────────────────────────────────────────────
BETTER_AUTH_SECRET=MuZmPjxaAxALgft1g0Zr2YZXWK3FF9HRs3ESlXzsLP8=
# Ganti dengan domain production Anda saat live (misal: https://kasirtokomu.vercel.app)
BETTER_AUTH_URL=http://localhost:3000

# ── Google Gemini AI ────────────────────────────────────────────────────────
# Isi dengan API Key dari: https://aistudio.google.com/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
GEMINI_TEXT_MODEL=gemini-2.0-flash
GEMINI_VISION_MODEL=gemini-2.0-flash

# ─────────────────────────────────────────────────────────────────────────────
# LANGKAH DEPLOY
# ─────────────────────────────────────────────────────────────────────────────
#
# 1. TERAPKAN SKEMA KE DATABASE (wajib setelah clone / update pertama kali)
#    Perintah ini akan sinkronisasi struktur tabel dari kode ke database cloud.
#
#    npm run db:push
#
# ─────────────────────────────────────────────────────────────────────────────
#
# 2. (OPSIONAL) ISI DATA AWAL / DUMMY
#    Jika database masih kosong dan ingin langsung ada produk, shift, dll:
#
#    npm run db:seed
#
# ─────────────────────────────────────────────────────────────────────────────
#
# 3. (OPSIONAL) TRANSFER DATA DARI LOKAL KE CLOUD
#    Jika ingin menarik/memindahkan data dari database lokal ke cloud InsForge,
#    buka file scripts/transfer-data.ts, sesuaikan SOURCE_DB_URL dan TARGET_DB_URL,
#    lalu jalankan:
#
#    npx tsx scripts/transfer-data.ts
#
# ─────────────────────────────────────────────────────────────────────────────
#
# 4. BUILD & JALANKAN
#    Untuk VPS / Node.js hosting biasa:
#
#    npm install
#    npm run build
#    npm run start
#
#    Untuk Vercel: cukup hubungkan repo GitHub ke Vercel,
#    set semua variabel di atas di dashboard Vercel, dan deploy otomatis.
#
# ─────────────────────────────────────────────────────────────────────────────
